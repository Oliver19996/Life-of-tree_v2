services:
  - type: web
    name: tree-of-life
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: APP_SECRET
        generateValue: true
      - key: ALLOW_DEMO_LOGIN
        value: "true"
